The data came through on a wide window, if the window is too narrow it gets compressed. When no data is available it comes through as NAN. Close dates are not formatted as short dates and include midnight time stamps.
Fixed missing data by using set option
Req # comes through as an object as does posting close date. 
Salary range as a string makes sense as it exists in the tracker as value ranges; idea to maybe split into 2 columns lower and upper and create as an integer
